{
    "require-dev": {
        "vimeo/psalm": "^4.30",
        "psalm/plugin-phpunit": "^0.16.1"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
