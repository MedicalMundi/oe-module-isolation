{
    "require-dev": {
        "vimeo/psalm": "^4.9",
        "psalm/plugin-phpunit": "^0.16.1"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
